{
  "name": "playground-yarn-hoisting",
  "description": "A playground to experiment with yarn hoisting behavior",
  "author": "Andrew Stegmaier",
  "license": "ISC",
  "private": true,
  "scripts": {
    "clean:windows": "if exist node_modules rmdir /s /q node_modules && if exist yarn.lock del yarn.lock",
    "clean:bash": "rm -rf ./node_modules/ && rm yarn.lock"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
